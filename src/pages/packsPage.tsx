// import { useDispatch, useSelector } from 'react-redux';
import { CategoryPack } from '../components/card/categoryPack';
// import * as ac from '../components/reducer/action-creator';
// import { iState } from '../store/store';
export function Packs() {
  //  const characters = useSelector((state: iState) => state.user);
  //  const dispatch = useDispatch();

  // //  useEffect(() => {
  // //    createProducts().then((characters) => {
  // //      dispatch(ac.(characters));
  // //    });
  // //  }, [dispatch]);
  return (
    <>
      <CategoryPack></CategoryPack>
    </>
  );
}
export default Packs;
